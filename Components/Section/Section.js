import { Asmr } from "..";
import styles from "./Section.module.css";

const Section = () => {
	return (
		<section className={styles.container}>
			<Asmr />
		</section>
	);
};

export default Section;
